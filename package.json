{
  "name": "reaction_tokens",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ reaction_tokens",
    "create-local": "graph create --node http://localhost:8020/ reaction_tokens",
    "remove-local": "graph remove --node http://localhost:8020/ reaction_tokens",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 reaction_tokens"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
